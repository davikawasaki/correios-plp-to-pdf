class PLP {
    constructor({
        accessData, sender, postalObjects
    } = {}) {
        this.accessData = accessData
        this.sender = sender
        this.postalObjects = postalObjects
    }
}
